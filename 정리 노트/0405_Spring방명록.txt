=================================
context-3-dao
=================================
스프링 프로젝트 기본 접근 순서
	1) VO생성
	2) DAO클래스 생성 및 BEAN객체 생성
	3) DAO에서 참조할 MAPPER정의
	4) Controller생성 및 servlet-context.xml에서 dao참조
	5) 컨트롤러에서 호출된 매핑을 통해 메서드 실행
		-> 바인딩, 포워딩 실행
	6) 포워딩 된 jsp에서 결과처리

=================================
ajax